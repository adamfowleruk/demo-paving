apiVersion: v1
kind: Service
metadata:
  name: {{ include "adsb-live-processor-java.fullname" . }}
  labels:
    {{- include "adsb-live-processor-java.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    {{- include "adsb-live-processor-java.selectorLabels" . | nindent 4 }}
