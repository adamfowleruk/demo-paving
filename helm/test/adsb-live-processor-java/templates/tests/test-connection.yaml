apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adsb-live-processor-java.fullname" . }}-test-connection"
  labels:
    {{- include "adsb-live-processor-java.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adsb-live-processor-java.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
